# Evaluation Input

Place the documents to compare here:

```text
eval-input/reference.docx
eval-input/generated.docx
```

Then run:

```bash
npm run evaluate
```

The repository includes synthetic fixture documents only. Do not place sensitive customer, patient, proprietary, PHI, PII, or credential material in committed files.
